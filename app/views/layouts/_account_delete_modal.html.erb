<div class="modal fade" id="accountDeleteModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel"
    aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel"><%= t('modal.account_delete_modal.confirm_message') %></h5>
                <button class="close" type="button" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">×</span>
                </button>
            </div>
            <div class="modal-body"><%= t('modal.account_delete_modal.description') %></div>
            <div class="modal-footer">
                <button class="btn btn-secondary" type="button" data-dismiss="modal"><%= t('modal.common.cancel_button') %></button>
                <%= link_to t('modal.account_delete_modal.delete_button'), welcome_path(current_user), method: :delete , class: 'btn btn-danger'%>
            </div>
        </div>
    </div>
</div>